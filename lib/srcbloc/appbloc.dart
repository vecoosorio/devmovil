import 'package:devmovil/srcbloc/screens/login_screens.dart';
import 'package:flutter/material.dart';

class AppBloc extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new MaterialApp(
        home: Scaffold(
      body: LoginScrenn(),
    ));
  }
}
